{
  "name": "henosis-platform",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @henosis/core test",
    "check": "pnpm build && pnpm test"
  },
  "devDependencies": { "typescript": "5.9.3" }
}
